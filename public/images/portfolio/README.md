# Portfolio Images

Drop `.jpg`, `.jpeg`, `.png`, or `.webp` files in this folder to have them
appear automatically in the Image Portfolio masonry grid — no code changes
needed. When this folder is empty, the section shows a "Images coming soon"
placeholder instead.
