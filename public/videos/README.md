# Video Files

Place real `.mp4` video files in this folder, then update `/src/data/resume.ts`.

In the `videos` array, set the `src` field to the filename (e.g. `"/videos/demo.mp4"`).
When `src` is `null`, the placeholder card is shown automatically.
